# ── SharkPay Checkout - Ambiente de Produção ──────────────────

# ── App configuration ──────────────────────────────────────────
VITE_SITE_NAME="SharkPay Checkout"
VITE_SITE_URL=https://seu-dominio.vercel.app

# ── Supabase (Essencial para o Admin e Persistência) ──────────
# Pegue no painel do Supabase -> Project Settings -> API
VITE_SUPABASE_URL=https://sua-url.supabase.co
VITE_SUPABASE_ANON_KEY=sua-chave-anon-publica

# ── N8N (Integração de Pagamento e Automação) ──────────────────
# URL do seu webhook de produção no n8n.
# Este webhook deve receber os dados do checkout para processar pagamentos e disparar e-mails.
VITE_N8N_WEBHOOK_URL=https://n8n.seu-dominio.com/webhook/identificador-da-automacao

# ── Stripe (Opcional - se usar o componente direto) ────────────
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_...

# ── PushinPay (Opcional - via n8n geralmente) ──────────────────
# VITE_PUSHINPAY_API_KEY=...

# ── OpenRouter LLM (Agente de Integrações) ────────────────────
# Gere sua chave em https://openrouter.ai/keys
# Usa modelos gratuitos para diagnóstico e integração
VITE_OPENROUTER_API_KEY=sk-or-v1-sua-chave-aqui

# ── Notas de Produção ──────────────────────────────────────────
# 1. Certifique-se de configurar estas variáveis no painel da Vercel (Settings -> Environment Variables).
# 2. O Admin utiliza o Supabase Auth. Configure as URLs de redirecionamento no Supabase.
# 3. Para pagamentos de teste, use as chaves 'test' do Stripe/PushinPay.
# 4. A chave do OpenRouter é usada pelo Agente IA no painel admin.
